<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ThisAppointmentApp"
             mc:Ignorable="d"
             x:Class="ThisAppointmentApp.DayOverview">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding DateTime, StringFormat='{0:T}'}" 
                           TextColor="black" Grid.Column="1" FontSize="20" HorizontalTextAlignment="Center">
                <Label.BindingContext>
                    <local:ClockViewModel/>
                </Label.BindingContext>
            </Label>
            <ListView x:Name="list" RowHeight="100">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Name}"/>
                                <Label Text="{Binding StartTime, StringFormat='{0:hh\\:mm}'}"/>
                                <Label Text="{Binding EndTime, StringFormat='{0:hh\\:mm}'}"/>
                                <Label Text="{Binding Location}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>